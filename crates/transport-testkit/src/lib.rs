//! Reusable conformance checks for Fungi transport implementations.
//!
//! Test consumers can use this crate as a development dependency. Consumers
//! needing support binaries can enable it through an optional dependency.

pub mod testkit;
